language: cpp
dist: trusty
sudo: false

addons:
  apt:
    packages:
    - valgrind
    - libgtest-dev
    - libgoogle-glog-dev

matrix:
  include:
    - os: linux
      compiler: gcc

script:
  - make
  - make check-style
